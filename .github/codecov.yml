codecov:
  notify:
    wait_for_ci: true
coverage:
  status:
    project:
      caikit_nlp_client:
        paths: "src/"
        target: auto
        threshold: 2%
github_checks:
  annotations: false
